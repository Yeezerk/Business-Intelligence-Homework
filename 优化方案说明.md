# 项目优化方案说明

> 基于当前实验状态（2026-06-07，4模型对比完成）的后续优化方向评估

---

## 一、当前状态速览

| 模型 | AUPRC | F1 | Cost | ECE | 核心问题 |
|------|-------|-----|------|-----|----------|
| XGBoost | 0.9331 | 0.8750 | 215 | 0.0006 | 基线，精度最优但召回最低 |
| DeepFM | 0.9174 | 0.8950 | 149 | 0.0439 | 深度最佳，ROCAUC最高 |
| FT-Transformer | 0.8738 | 0.8930 | 167 | 0.1823 | **注意力塌缩为均匀分布**，校准极差 |
| TabNet | 0.8642 | 0.8826 | 187 | 0.0007 | 深度最差，但校准优秀 |

**关键发现**：
- FTT 的 Softmax 自注意力在 PCA 匿名化特征空间中 **退化为一视同仁的平均投票**（29特征的注意力值全部落在 [0.0327, 0.0342]，标准差 0.0004）
- DeepFM 的显式二阶内积交互反而在 PCA 空间中更稳健
- XGBoost 的轴对齐分裂天然适合 PCA 特征——每个分裂沿单一维度，不受其他成分干扰

---

## 二、三个优化方向的评估

### 方向 1：图网络（GNN）

**思路**：将交易构建为图结构（节点=交易/用户/商户，边=共享属性/时序关系），用 GCN/GAT 等图神经网络建模。

**可行性评估：❌ 不可行**

| 维度 | 判断 | 原因 |
|------|------|------|
| 数据条件 | ❌ | 当前数据集 V1-V28 是完全匿名的 PCA 成分，无用户ID、商户ID、设备指纹、IP地址等实体信息 |
| 图构建 | ❌ | 图网络需要边（edges）——共享商户、共享设备、交易时序链等。PCA 仅保留方差最大的线性组合方向，图结构信息已不可逆地丢失 |
| 学术合理性 | ❌ | 在无图结构的数据上强行构造图（如 kNN 图）是常见但学术上可疑的操作，审稿人会质疑"为什么这个数据集适合图建模" |
| 时间成本 | ❌ | GNN 实现+调参+图构建+训练需 1-2 周，但收益无法保证 |

**结论：不建议。**GNN 是解决"有实体+有关系"场景的方法，而非当前 PCA 匿名化数据的问题。若未来换用带实体信息的原始数据集（如 IEEE-CIS Fraud Detection），GNN 将是高优先级方向。

---

### 方向 2：FT-Transformer 超参数调优

**思路**：针对 FTT 注意力塌缩和校准差的问题，通过系统调参提升性能。

**可行性评估：✅ 可行，收益中等**

**具体方案**：

| 参数 | 当前值 | 调优范围 | 预期效果 |
|------|--------|----------|----------|
| `n_heads` | 8 | {2, 4, 8} | 减少头数可能迫使每个头学习更有意义的模式。2头→每头 dim=32，在 29 维特征上更集中 |
| `n_layers` | 3 | {1, 2, 3, 4} | 浅层网络可能减少注意力退化。1-2层在低维 PCA 空间可能足够 |
| `d_model` | 64 | {32, 48, 64} | 小嵌入更紧凑，减少冗余参数。32维对 29 维输入已足够 |
| `dropout` | 0.1 | {0.1, 0.2, 0.3} | 提高 dropout 可能缓解对少数特征的过度依赖 |
| `learning_rate` | 5e-4 | {1e-4, 2e-4, 5e-4, 1e-3} | 更低 lr 可能减少 Focal Loss α=0.995 引起的梯度震荡 |
| **Focal Loss α** | 0.995 | {0.25, 0.50, 0.75, 0.90} | **关键**：α=0.995 导致少数类梯度权重 ~200×，可能是注意力塌缩和校准差的根因。α=0.25 或 0.50 可能让训练更稳定 |
| **残差连接** | Pre-Norm | Post-Norm + 更小 init | Post-Norm 可能提供更稳定的训练动态 |

**预估提升空间**：
- FTT AUPRC 当前 0.8738，保守估计可提升至 0.89-0.90（+2-3pp）
- ECE 可通过 α 调低 + Platt Scaling 降至 0.05 以下
- 但注意：DeepFM 当前已达 0.9174，FTT 即使优化后也未必能超越

**风险**：
- 超参调优不解决"PCA空间中注意力缺乏语义锚点"的根本问题——Softmax 的归一化特性意味着即使调参，注意力分布可能仍然较均匀
- 调优过程涉及 7 个参数组合，全排列需训练 3×4×3×3×4×4 = 1728 次，须用网格搜索或 Optuna 智能采样（推荐 30-50 trials）

**结论：建议做，但优先级中等。** 核心收益不在"让 FTT 超越 DeepFM"，而在"通过控制变量实验揭示注意力塌缩的根因"——这对论文的分析深度有重要贡献。

---

### 方向 3：神经网络学习 XGBoost 分裂策略（⭐ 推荐）

**思路**：用 XGBoost 的决策边界作为监督信号训练神经网络。这并非简单的知识蒸馏（distillation），而是**树监督学习（Tree-supervised Neural Networks）**——让神经网络学习树模型的**归纳偏置**而非仅模仿输出。

**可行性评估：✅ 可行，收益最高，学术价值最大**

**具体实现方案**：

#### 3.1 方案 A：Leaf-based Soft Label Distillation（简单，1天可完成）

```
XGBoost 训练 → 对每个样本输出叶子节点分布（soft label）
             → 神经网络以 XGBoost 的 soft label 为回归目标
             → 网络学习树模型的特征利用模式
```

- **实现难度**：低。只需修改训练 loop 的 target
- **学术贡献**：验证"树模型的 soft label 能否将轴对齐归纳偏置传递给神经网络"
- **当前代码改动**：新增 15 行代码（训练时用 `xgb_model.predict(X, pred_leaf=True)` 获取叶子索引，转为 soft probability 分布作为 target）

#### 3.2 方案 B：Feature Split Importance Regularization（中等，2-3天）

```
XGBoost 训练 → 提取每个特征的 split gain（分裂增益）作为重要性权重
             → 神经网络训练时，对特征嵌入层施加正则化：
                 - 高 gain 特征 → 允许更大嵌入范数
                 - 低 gain 特征 → 施加 L1 惩罚收缩
             → 引导网络将参数量分配给 XGBoost 认为重要的维度
```

- **实现难度**：中。需要在 FTT/DeepFM 的嵌入层增加 feature-weighted regularization
- **学术贡献**：提出"树模型特征重要性引导的神经网络训练"——一种新的归纳偏置迁移方式

#### 3.3 方案 C：Tree-Structured Neural Attention（较难，1周）

```
XGBoost 训练 → 提取每棵树的 split 路径（feature × threshold 序列）
             → 在神经网络中构造"树注意力"模块：
                 - 每个注意力头对应一棵 XGBoost 树的 split 结构
                 - 初始化时，注意力权重与树的分裂增益对齐
                 - 训练时微调，但保持树结构的归纳偏置
             → 形成 XGBoost prior + NN fine-tuning 的混合模型
```

- **实现难度**：高。需深刻理解 XGBoost 内部结构和 PyTorch 自定义模块
- **学术贡献**：最大学术创新点——"将梯度提升树的轴对齐分裂策略显式嵌入 Transformer 注意力机制"

**推荐实施优先级：A → B → C（渐进式）**

先做 A 验证概念，若有效（AUPRC 提升 >2pp）则推进 B，C 作为论文的核心创新点在后期打磨。

---

## 三、三类双向的横向对比

| 对比维度 | 方向1：GNN | 方向2：FTT调参 | 方向3：树监督学习 |
|----------|-----------|---------------|-------------------|
| 数据可行性 | ❌ 不可行 | ✅ 可行 | ✅ 可行 |
| 实现难度 | 高 | 低-中 | 中（方案A低） |
| 预期 AUPRC 提升 | — | +2-3pp | +3-5pp（方案A预估） |
| 学术创新度 | 低（强行GNN） | 中（调参实验） | **高**（树→NN归纳偏置迁移） |
| 论文贡献增量 | 弱 | 中等 | **强**（可成核心创新点） |
| 时间投入 | 1-2周 | 2-3天 | 方案A:1天, B:2-3天, C:1周 |
| 与现有实验兼容 | 不符 | 完全兼容 | 完全兼容 |

---

## 四、关于"类别不平衡处理"是否删除的分析

### 当前内容

消融实验包含 4 种策略（Focal Loss / Class Weight / SMOTE / Plain CE），以手写 TabNet 为骨干。

### 删除的理由
- 消融使用手写 TabNet 而主实验用 pytorch-tabnet，骨干不一致是硬伤（审稿人已指出）
- 4 种策略的 AUPRC 差距 <1pp（0.8676--0.8737），"Focal Loss 微弱领先"的结论较弱
- SMOTE 在 PCA 空间的失败虽然有趣，但本质上是"SMOTE 不适合 PCA 数据"的常识验证

### 保留的理由
- SMOTE 失败（0.7839，↓ 9pp）是论文**最有区分度的消融发现**——A、B、C 三个方案不会产生这种程度的信号
- Plain CE 的竞争力暗示了"架构本身的类别鲁棒性"，这与方案 3 的"树监督学习是否需要额外不平衡处理"可形成呼应
- 消融实验是当前论文章节结构的重要组成部分，删除后 experiments.tex 只剩模型对比表，结构失衡

### ✅ 推荐方案：保留但重构

**重构为以下形式（二选一）：**

**子方案 1：精简为 2 行对比**
- 保留：Focal Loss vs Plain CE（二元对比，结论更清晰）
- 删除：Class Weight（与 Focal Loss 几乎等效）和 SMOTE（独立成"PCA空间分析"段落）
- 将 SMOTE 的失败重命名为"合成样本在匿名化特征空间中的风险"，提升为方法论的警示而非简单消融

**子方案 2：转移到新模型上重做**
- 若实施方案 3（树监督学习），用新模型作为消融骨干
- 只需对比 2 种策略（Focal Loss vs Plain CE），验证"树监督学习是否自带不平衡鲁棒性"
- 工作量增加约 2 小时训练时间

### 推荐：子方案 1（精简）+ 子方案 2（若采用方案 3 则执行）

---

## 五、综合推荐方案

```
优先级排序：

P0（立即执行）：
├─ 方案 3A：Leaf-based Soft Label Distillation
│   工作量：1天  预期收益：AUPRC +3-5pp，最强学术创新点
│
P1（P0 完成后执行）：
├─ 方案 2（部分）：Focal Loss α 调优 + FTT 架构简化
│   工作量：2天  预期收益：FTT 性能提升 + 注意力塌缩根因分析
│
P2（视 P0 结果决定）：
├─ 方案 3B：Feature Split Importance Regularization
│   工作量：2-3天  预期收益：深化树→NN 归纳偏置迁移的理论贡献
│
P3（可选，创新点足够时可不做）：
├─ 方案 3C：Tree-Structured Neural Attention
│   工作量：1周  预期收益：论文核心创新模块
│
└─ 类别不平衡处理：精简重构为二元对比 + SMOTE 独立分析
    工作量：1小时代码 + 1小时论文修改
```

### 最终推荐的 3 个模型对比结构

```
优化后论文的模型对比（4→4，但内容升级）：

1. XGBoost（不变）             — 传统基线
2. DeepFM（不变）               — 深度模型中已验证最优
3. FT-Transformer（优化后）     — 调 α + 简化架构，含注意力塌缩分析
4. Tree-Supervised NN（新增★）  — 核心创新点：树→NN 归纳偏置迁移
```

TabNet 可以从主对比表移到"消融/补充分析"中（或保留但注明使用官方库，与其他手写模型区分）。

---

## 六、下一步行动

| 步骤 | 操作 | 预计耗时 |
|------|------|----------|
| 1 | **用户确认方向**：是否接受"方案 3A + 方案 2 部分 + 消融精简"的推荐？ | 即时 |
| 2 | 实现方案 3A：Leaf-based Soft Label Distillation | 1天 |
| 3 | 实现方案 2 部分：FTT α 参数扫描 | 1天 |
| 4 | 重训练 + 重评估 | 1-2小时（GPU） |
| 5 | 消融实验精简：Focal vs Plain CE 二元对比 | 2小时 |
| 6 | 更新论文文字（experiments + methodology + analysis） | 2小时 |
| 7 | 编译 + nature-reviewer 二审 | 1小时 |

---

> **说明**：以上分析基于 2026-06-07 完成的 4 模型 GPU 训练结果和 Nature 审稿反馈。三个方向中，方案 3（树监督学习）学术创新度最高且实现成本可控，方案 2（FTT 调参）作为补充分析显著增强实验深度，方案 1（GNN）在当前 PCA 匿名化数据上不可行。类别不平衡消融建议保留核心发现但精简结构。
